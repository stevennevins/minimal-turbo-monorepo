{
  "name": "my-turbo-repo",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/node": "^18.17.1",
    "turbo": "^1.10.12",
    "typescript": "^5.1.6"
  }
}
